import 'package:flutter/material.dart';

class Wecode extends StatefulWidget {
  const Wecode({ Key? key }) : super(key: key);

  @override
  _WecodeState createState() => _WecodeState();
}

class _WecodeState extends State<Wecode> {
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}